integrations:
    hub:
      - integrationName: gm_hub
        type: dockerRegistryLogin

build:
    ci:
        - docker build -t gmshippable .
        - echo `pwd`
        - docker run -t -v `pwd`:/test gmshippable:latest
